import AgGrid from "@/components/markdown-styles/AgGrid"

# 01. Basic

## Basic

기본 형태는 다음과 같다.

```jsx
import { AgGridReact } from "ag-grid-react";
import "ag-grid-enterprise/styles/ag-grid.css";
import "ag-grid-enterprise/styles/ag-theme-alpine.css";

export default function Default() {
  const columns = [{ field: "name" }, { field: "familyName" }];
  const rowDatas = [
    { name: "Hello", familyName: "World" },
    { name: "Lorem", familyName: "Ipsum" },
  ];

  return (
    <div style={{ height: "100%", boxSizing: "border-box" }}>
      <div
        style={{ width: "100%", height: 200 }}
        className="ag-theme-alpine"
      >
        <AgGridReact columnDefs={columns} rowData={rowDatas} />
      </div>
    </div>
  );
}
```

export const columns = () => [{ field: "name" }, { field: "familyName" }];
export const rowDatas = () => [
  { name: "Hello", familyName: "World" },
  { name: "Lorem", familyName: "Ipsum" },
];

<div style={{ height: "200px", boxSizing: "border-box" }}>
  <div
    style={{ width: "100%", height: "100%" }}
    className="ag-theme-alpine"
  >
    <AgGrid
      columnDefs={columns()}
      rowData={rowDatas()}
    />
  </div>
</div>

## Miscs

* `valueGetter`로 표현할 값을 지정해줘야 cell에 우클릭 후 copy to clipboard시 해당 값이 copy된다. <br />
    ```jsx
    {
      headerName: "custom-data",
      width: 250,
      cellRendererSelector: (params) => ({
        component: () => <Custom smiles={params.data.custom} />,
      }),
      valueGetter: (params) => params.data.custom, // 이 부분이 없어도 표현에는 문제 없지만, copy 시 빈 값이 copy된다.
    }
    ```